import 'package:flutter/material.dart';

class ScreenResLogin extends StatelessWidget {
  const ScreenResLogin({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Hello"),
      ),
    );
  }
}